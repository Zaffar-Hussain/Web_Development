<!DOCTYPE html>
<html>
<head>
	<title>REFERENCE</title>
</head>
<body>
	<h1>PDO Information</h1>
	<h4>
		PDO::beginTransaction — Initiates a transaction<br>
PDO::commit — Commits a transaction<br>
PDO::__construct — Creates a PDO instance representing a connection to a database<br>
PDO::errorCode — Fetch the SQLSTATE associated with the last operation on the database handle<br>
PDO::errorInfo — Fetch extended error information associated with the last operation on the database handle<br>
PDO::exec — Execute an SQL statement and return the number of affected rows<br>
PDO::getAttribute — Retrieve a database connection attribute<br>
PDO::getAvailableDrivers — Return an array of available PDO drivers<br>
PDO::inTransaction — Checks if inside a transaction<br>
PDO::lastInsertId — Returns the ID of the last inserted row or sequence value<br>
PDO::prepare — Prepares a statement for execution and returns a statement object<br>
PDO::query — Executes an SQL statement, returning a result set as a PDOStatement object<br>
PDO::quote — Quotes a string for use in a query<br>
PDO::rollBack — Rolls back a transaction<br>
PDO::setAttribute — Set an attribute<br>
PDOStatement — The PDOStatement class<br>
PDOStatement::bindColumn — Bind a column to a PHP variable<br>
PDOStatement::bindParam — Binds a parameter to the specified variable name<br>
PDOStatement::bindValue — Binds a value to a parameter<br>
PDOStatement::closeCursor — Closes the cursor, enabling the statement to be executed again<br>
PDOStatement::columnCount — Returns the number of columns in the result set<br>
PDOStatement::debugDumpParams — Dump an SQL prepared command<br>
PDOStatement::errorCode — Fetch the SQLSTATE associated with the last operation on the statement handle<br>
PDOStatement::errorInfo — Fetch extended error information associated with the last operation on the statement handle<br>
PDOStatement::execute — Executes a prepared statement<br>
PDOStatement::fetch — Fetches the next row from a result set<br>
PDOStatement::fetchAll — Returns an array containing all of the result set rows<br>
PDOStatement::fetchColumn — Returns a single column from the next row of a result set<br>
PDOStatement::fetchObject — Fetches the next row and returns it as an object<br>
PDOStatement::getAttribute — Retrieve a statement attribute<br>
PDOStatement::getColumnMeta — Returns metadata for a column in a result set<br>
PDOStatement::nextRowset — Advances to the next rowset in a multi-rowset statement handle<br>
PDOStatement::rowCount — Returns the number of rows affected by the last SQL statement<br>
PDOStatement::setAttribute — Set a statement attribute<br>
PDOStatement::setFetchMode — Set the default fetch mode for this statement<br>
PDOException — The PDOException class<br>
PDO Drivers<br>
CUBRID (PDO) — CUBRID Functions (PDO_CUBRID)<br>
MS SQL Server (PDO) — Microsoft SQL Server and Sybase Functions (PDO_DBLIB)<br>
Firebird (PDO) — Firebird Functions (PDO_FIREBIRD)<br>
IBM (PDO) — IBM Functions (PDO_IBM)<br>
Informix (PDO) — Informix Functions (PDO_INFORMIX)<br>
MySQL (PDO) — MySQL Functions (PDO_MYSQL)<br>
MS SQL Server (PDO) — Microsoft SQL Server Functions (PDO_SQLSRV)<br>
Oracle (PDO) — Oracle Functions (PDO_OCI)<br>
ODBC and DB2 (PDO) — ODBC and DB2 Functions (PDO_ODBC)<br>
PostgreSQL (PDO) — PostgreSQL Functions (PDO_PGSQL)<br>
SQLite (PDO) — SQLite Functions (PDO_SQLITE)<br>
4D (PDO) — 4D Functions (PDO_4D)<br>
	</h4>
</body>
</html>